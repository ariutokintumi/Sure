# Kept minimal because processor sends notifications directly upon success/error.
# This module can be expanded to scan paid/ and error/ if you prefer that pattern.
print('[mailer_notify] Processor currently handles notifications inline. Nothing to run here.')